Analyze the following text and break it down sequentially into segments based on who is speaking or narrating. Assign one of three roles to each segment: 'MALE' (for male character dialogue), 'FEMALE' (for female character dialogue), or 'NARRATOR' (for author text or unnamed narrators). Return the result STRICTLY as a valid JSON array of objects, where each object has 'role' and 'text' fields. Do not include markdown code blocks (like ```json), just return the raw JSON array. Ensure the combined text of all segments perfectly matches the original text (including punctuation). Original text:

